# Quiz: complejidad de operaciones con listas

¿Cuál es la **complejidad temporal** (notación Big-O) de ejecutar `lista.append(x)` en una
lista de Python cuando la lista ya tiene `n` elementos?

Selecciona una sola opción.