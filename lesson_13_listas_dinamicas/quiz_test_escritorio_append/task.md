# Quiz de escritorio: append vs insert

## Pregunta

¿Que imprime este codigo?

```python
lst = ["a", "b", "c"]
lst.append("d")
print(lst)
```

## Pista

`append` agrega al **final**, no al inicio. `lst.append("d")` agrega `"d"` despues de `"c"`.