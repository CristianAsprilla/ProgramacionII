# Quiz de escritorio: not y or

## Pregunta

¿Que imprime este codigo?

```python
print(not (True or False))
```

## Pista

`or` da `True` si al menos un operando es `True`. `True or False` = `True`. Luego `not(True)` = ?