# Quiz de escritorio: operadores logicos

## Pregunta

¿Que imprime este codigo?

```python
print((5 > 3) and (2 < 1))
```

## Pista

`and` da `True` solo si ambos operandos son `True`. `(5 > 3)` es `True` y `(2 < 1)` es `False`, asi que `True and False` = ?