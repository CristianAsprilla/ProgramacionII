# Quiz de escritorio: bucle for

## Pregunta

¿Que imprime este codigo?

```python
for i in range(3):
    print(i, end=" ")
```

## Pista

`range(3)` genera `0, 1, 2`. El argumento `end=" "` en `print` evita el salto de linea y agrega un espacio entre los numeros.