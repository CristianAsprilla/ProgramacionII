# Primeros N números de Fibonacci

Completa `fibonacci(n)` para devolver una lista con los primeros `n` términos, comenzando por `0, 1`. `imprimir_fibonacci(n)` los muestra separados por espacios. Para `n = 0`, devuelve una lista vacía; no elimines la validación de negativos.
