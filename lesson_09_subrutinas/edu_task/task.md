# Módulo `geometria`

Completa `geometria.py` con `area_circulo(radio)` y `area_rectangulo(base, altura)`. `main.py` debe importarlas y usarlas en `resumen_areas`. Conserva las validaciones para medidas negativas.
