# Quiz de escritorio: import

## Pregunta

¿Que imprime este codigo?

```python
from math import sqrt
print(sqrt(16))
```

## Pista

`from math import sqrt` importa la funcion `sqrt` del modulo `math`. `sqrt(16)` calcula la raiz cuadrada, que es 4 (en Python 3 retorna float).