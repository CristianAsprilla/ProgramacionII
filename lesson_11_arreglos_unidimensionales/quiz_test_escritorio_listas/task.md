# Quiz de escritorio: append en listas

## Pregunta

¿Que imprime este codigo?

```python
lst = [1, 2, 3]
lst.append(4)
print(lst)
```

## Pista

`append` modifica la lista original agregando el elemento al **final**. `lst.append(4)` convierte `[1, 2, 3]` en `[1, 2, 3, 4]`.