"""Cuenta la frecuencia de cada palabra en un texto."""


def contar_frecuencia(texto):
    """Cuenta cuantas veces aparece cada palabra en un texto.

    Args:
        texto (str): texto a analizar.

    Returns:
        dict: diccionario {palabra: frecuencia}.
    """
    # TODO: divide el texto en palabras y cuenta cuantas veces aparece cada una
    conteo = {}
    return conteo


if __name__ == "__main__":
    muestra = "el gato come pescado el perro come carne"
    print(contar_frecuencia(muestra))
