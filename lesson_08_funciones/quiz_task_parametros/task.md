# Parámetros predeterminados en acción

Analizá `main.py`. La función `clasificar` recibe una `edad` y, por defecto, considera que la persona es estudiante. ¿Qué imprime el programa al ejecutar las dos llamadas?