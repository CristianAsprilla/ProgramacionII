def factorial(n: int) -> int:
    """Calcula n! para un entero no negativo.

    Ejemplos:
    >>> factorial(0)
    1
    >>> factorial(5)
    120

    Si n es negativo, lanzar ValueError con el mensaje "N debe ser no negativo".
    """
    # TODO: calcula y devuelve n factorial
    return 1
