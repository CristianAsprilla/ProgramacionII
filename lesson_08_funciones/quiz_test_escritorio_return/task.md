# Quiz de escritorio: return basico

## Pregunta

¿Que imprime este codigo?

```python
def doble(x):
    return x * 2

print(doble(5))
```

## Pista

Una funcion que termina con `return valor` devuelve ese valor a quien la llamo. `print()` muestra el valor devuelto.