"""Calcula el area de un circulo."""


def area_circulo(radio):
    """Calcula el area de un circulo dado su radio.

    Args:
        radio (float): radio del circulo (en unidades).

    Returns:
        float: area del circulo.
    """
    # TODO: usa math.pi y la formula area = pi * r**2
    return 0.0


if __name__ == "__main__":
    print(area_circulo(1))      # ~3.14159
    print(area_circulo(2))      # ~12.566
    print(area_circulo(0))      # 0.0